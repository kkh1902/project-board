spring:
  datasource:
    url: jdbc:mysql://localhost:3307/project-board
    username: uno
    password: thisisTESTpw!#%&
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: never  # 테스트에서 data.sql 자동 실행 막기
